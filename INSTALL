epiphany-editor depends on python2.3 or greater, pygame, and pygtk2. Please install them first.

On Debian or Ubuntu try this:

sudo apt-get install python python-gtk2 python-pygame

Then you may run the editor:

./epiphany-editor

Or you may symlink it in to your path, like:

ln -s `pwd`/epiphany-editor /usr/bin/

